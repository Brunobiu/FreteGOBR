/**
 * Stub de tipos para `leaflet-rotate`.
 *
 * O modulo nao publica `.d.ts`. Importamos apenas pelo side-effect
 * (extende `L.Map.prototype` com `rotate`/`touchRotate`/`bearing`).
 * Nada do default export e usado em runtime.
 */
declare module 'leaflet-rotate';
