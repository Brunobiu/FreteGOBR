/**
 * Página pública de Termos de Uso (Feature 1 — legal). Rota `/termos`.
 */
import LegalPage from '../components/legal/LegalPage';

export default function TermosPage() {
  return <LegalPage doc="terms" />;
}
