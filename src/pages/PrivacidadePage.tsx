/**
 * Página pública de Política de Privacidade (Feature 1 — legal).
 * Rota `/privacidade`.
 */
import LegalPage from '../components/legal/LegalPage';

export default function PrivacidadePage() {
  return <LegalPage doc="privacy" />;
}
