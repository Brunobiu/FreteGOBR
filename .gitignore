# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Cobertura de testes (gerada localmente; nunca commitar)
coverage/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.production

# Test credentials (local only)
logins

# Pasta com credenciais locais (Bruno)
Credencial/

# Pasta com credenciais locais
Credencial/


# Capacitor / Mobile
android/.gradle/
android/.idea/
android/build/
android/app/build/
android/captures/
android/local.properties
android/*.iml
android/app/*.iml
android/app/release/
android/app/debug/
android/keystore.properties
android/**/google-services.json
# Firebase service account / admin SDK keys (qualquer JSON com esse padrao)
**/firebase-adminsdk-*.json
**/*-firebase-adminsdk-*.json
firebase-service-account.json
ios/App/App.xcworkspace/xcuserdata/
ios/App/Pods/
ios/App/build/
ios/App/.gradle/

# Capacitor build outputs
*.apk
*.aab
*.ipa
*.keystore

# Capacitor sync (regenerados)
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
android/capacitor-cordova-android-plugins/

# Supabase CLI temporary state (generated locally; never commit)
supabase/.temp/

# Local build artifacts/outputs (ad-hoc captures)
build_out.txt

# Backups do banco (gerados localmente; contem dados — NUNCA commitar)
backups/
*.sql.gz
db_backup_*.sql

# Ferramentas portáteis baixadas localmente (ex: pg_dump portátil)
tools/
